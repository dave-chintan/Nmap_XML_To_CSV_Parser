# Nmap_XML_To_CSV_Parser
I have modified an already existing script that parses the XML output from NMAP to CSV since it doesn't meet my requirements. This is a modified version of an existing parser. 
